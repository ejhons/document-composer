[project]
name = "doc-composer-desktop"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "doc-composer-core",
    "doc-composer-shared",
    "pyside6"
]

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"
